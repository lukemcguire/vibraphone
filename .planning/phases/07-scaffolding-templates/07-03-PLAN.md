---
phase: 07-scaffolding-templates
plan: 03
type: execute
wave: 2
depends_on:
  - 07-01
files_modified:
  - src/vibraphone/utils/template_loader.py
  - src/vibraphone/utils/__init__.py
autonomous: true
requirements:
  - TMPL-02

must_haves:
  truths:
    - Template files are loadable via importlib.resources after pip install
    - Template loader works with both single files and directory trees
    - Jinja2 templates are rendered with project-specific variables
  artifacts:
    - path: src/vibraphone/utils/template_loader.py
      provides: "Template loading via importlib.resources"
      exports: ["load_template", "load_template_tree", "render_template"]
  key_links:
    - from: src/vibraphone/utils/template_loader.py
      to: vibraphone.templates package
      via: importlib.resources.files()
      pattern: "resources.files.*vibraphone.templates"
---

<objective>
Create template_loader utility for loading bundled templates via importlib.resources.

Purpose: Templates must be accessible after pip install, not just from source checkout.
Output: Utility module with functions for loading and rendering templates.
</objective>

<execution_context>
@/home/luke/.claude/get-shit-done/workflows/execute-plan.md
@/home/luke/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-scaffolding-templates/07-CONTEXT.md
@.planning/phases/07-scaffolding-templates/07-RESEARCH.md

# Reference: importlib.resources patterns from RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create template_loader utility</name>
  <files>
    src/vibraphone/utils/template_loader.py
    src/vibraphone/utils/__init__.py
  </files>
  <action>
    Create the template loader utility following RESEARCH.md Pattern 1.

    Implementation for src/vibraphone/utils/template_loader.py:

    ```python
    """Template loading utilities using importlib.resources.

    Templates are bundled in the vibraphone.templates package and accessed
    via importlib.resources for compatibility with pip installed wheels.
    """

    from __future__ import annotations

    from importlib import resources
    from pathlib import Path
    from typing import Any

    from jinja2 import Environment, FileSystemLoader, select_autoescape


    def get_template_package() -> resources.abc.Traversable:
        """Get the vibraphone.templates package as a Traversable.

        Returns:
            Traversable representing the templates package root.
        """
        return resources.files("vibraphone.templates")


    def load_template(template_name: str) -> str:
        """Load a template file from the vibraphone.templates package.

        Uses importlib.resources for compatibility with zip installs and wheels.

        Args:
            template_name: Relative path within templates/ (e.g., "vibraphone.yaml.j2")

        Returns:
            Template content as string.

        Raises:
            FileNotFoundError: If template doesn't exist.
        """
        template_files = get_template_package()
        template_path = template_files / template_name

        if not template_path.is_file():
            msg = f"Template not found: {template_name}"
            raise FileNotFoundError(msg)

        return template_path.read_text(encoding="utf-8")


    def load_template_tree(subdir: str) -> dict[str, str]:
        """Load all templates from a subdirectory.

        Args:
            subdir: Subdirectory within templates/ (e.g., "docs")

        Returns:
            Dict mapping relative path to content for each file in the tree.
        """
        template_files = get_template_package()
        target = template_files / subdir

        templates: dict[str, str] = {}
        for item in target.rglob("*"):
            if item.is_file():
                rel_path = item.relative_to(target)
                templates[str(rel_path)] = item.read_text(encoding="utf-8")

        return templates


    def render_template(template_content: str, variables: dict[str, Any]) -> str:
        """Render a Jinja2 template with the given variables.

        Args:
            template_content: Raw template string with Jinja2 syntax.
            variables: Dict of variable names to values.

        Returns:
            Rendered template string.
        """
        env = Environment(
            autoescape=select_autoescape(default=False),
            keep_trailing_newline=True,
        )
        template = env.from_string(template_content)
        return template.render(**variables)


    def get_all_template_paths() -> list[str]:
        """Get list of all template file paths in the package.

        Returns:
            List of relative paths within templates/ directory.
        """
        template_files = get_template_package()
        paths: list[str] = []

        for item in template_files.rglob("*"):
            if item.is_file() and not str(item).endswith("__pycache__"):
                # Get path relative to templates package
                rel = item.relative_to(template_files)
                paths.append(str(rel))

        return paths
    ```

    Update src/vibraphone/utils/__init__.py to export the new functions:
    ```python
    from vibraphone.utils.template_loader import (
        get_all_template_paths,
        get_template_package,
        load_template,
        load_template_tree,
        render_template,
    )

    __all__ = [
        "get_all_template_paths",
        "get_template_package",
        "load_template",
        "load_template_tree",
        "render_template",
    ]
    ```

    Note: jinja2 is already a dependency (pulled in by instructor), no need to add to pyproject.toml.
  </action>
  <verify>
    python -c "from vibraphone.utils.template_loader import load_template, render_template; t = load_template('vibraphone.yaml.j2'); print(render_template(t, {'project_name': 'test'}))"
  </verify>
  <done>
    template_loader.py exists with load_template, load_template_tree, render_template functions working.
  </done>
</task>

</tasks>

<verification>
1. load_template() loads vibraphone.yaml.j2 from bundled templates
2. load_template_tree("docs") returns dict of docs templates
3. render_template() correctly substitutes Jinja2 variables
4. get_all_template_paths() lists all template files
5. Works after pip install (not just from source)
</verification>

<success_criteria>
- load_template("vibraphone.yaml.j2") returns template content
- render_template() correctly replaces {{ project_name }} with provided value
- load_template_tree("docs") returns all governance doc templates
</success_criteria>

<output>
After completion, create `.planning/phases/07-scaffolding-templates/07-03-SUMMARY.md`
</output>
