---
phase: 11-command-documentation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/vibraphone/commands/v.md]
autonomous: true
requirements: [SLASH-02, SLASH-04]
user_setup: []

must_haves:
  truths:
    - User can quickly look up any command in a scannable quick reference table
    - User sees commands organized in 4 workflow stage groups
    - Dict-heavy commands have expanded inline JSON examples
  artifacts:
    - path: src/vibraphone/commands/v.md
      provides: Reorganized command documentation
      contains:
        - "## Quick Reference"
        - "## Start Work"
        - "## Run Quality"
        - "## Commit & Merge"
        - "## Session Management"
        - "## Troubleshooting"
  key_links:
    - from: Quick Reference table
      to: All commands
      via: Command/Purpose/MCP Tool mapping
    - from: MCP Tool Calling Convention section
      to: Dict-heavy command examples
      via: WRONG/RIGHT pattern reinforcement
---

<objective>
Reorganize v.md document structure and add comprehensive documentation for
dict-heavy commands with expanded inline JSON examples.

Purpose: Establish the 4-group workflow organization and quick reference
table that mirrors how users work through tasks. Give dict-heavy commands
(init, configure-stack, import-plan) special attention with multiple JSON
examples for copy-paste modification.

Output: Restructured v.md with Quick Reference table, 4 workflow section
headers with intro paragraphs, and expanded dict-heavy command documentation.
</objective>

<execution_context>
@/home/luke/.claude/get-shit-done/workflows/execute-plan.md
@/home/luke/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-command-documentation/11-CONTEXT.md
@.planning/phases/11-command-documentation/11-RESEARCH.md
@src/vibraphone/commands/v.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Quick Reference table and reorganize document structure</name>
  <files>src/vibraphone/commands/v.md</files>
  <action>
    Restructure v.md with the following changes:

    1. **Add Quick Reference table** after the "MCP Tool Calling Convention"
       section (before the current "Commands" section):

       ```markdown
       ## Quick Reference

       | Command | Purpose | MCP Tool |
       | ------- | ------- | -------- |
       | /v init | Initialize vibraphone in project | vibraphone_init_project |
       | /v check-prereqs | Check dependencies | vibraphone_check_prerequisites |
       | /v configure-stack | Configure test/lint/format | vibraphone_configure_stack |
       | /v import-plan | Import GSD plans to Beads | vibraphone_import_gsd_plan |
       | /v list | List tasks from Beads | vibraphone_list_tasks |
       | /v next | Get next ready task | vibraphone_next_ready |
       | /v start &lt;id&gt; | Start task in worktree | vibraphone_start_task |
       | /v test | Run tests | vibraphone_run_tests |
       | /v lint | Run linter | vibraphone_run_lint |
       | /v format | Run formatter | vibraphone_run_format |
       | /v review | Request code review | vibraphone_request_code_review |
       | /v commit &lt;msg&gt; | Attempt commit | vibraphone_attempt_commit |
       | /v merge &lt;id&gt; | Merge task branch | vibraphone_merge_task |
       | /v cleanup &lt;id&gt; | Remove worktree/branch | vibraphone_cleanup_task |
       | /v complete &lt;id&gt; | Mark task done | vibraphone_complete_task |
       | /v status | Show session state | vibraphone_recover_session |
       | /v health | Show health metrics | vibraphone_health_check |
       | /v recover | Resume session | vibraphone_recover_session |
       | /v cycle | Run quality suite | (multiple) |
       | /v finish | Complete workflow | (multiple) |
       ```

    2. **Replace the flat "Commands" section** with 4 workflow stage sections:

       ```markdown
       ## Start Work

       Commands for discovering and starting tasks. These are your entry points
       into the workflow - use them to see what's available and begin working
       on a task.

       [Commands will be moved here in Task 2]

       ## Run Quality

       Commands for running quality gates. These enforce the core value of
       vibraphone: every code change goes through tests, lint, format, and
       review before it can be committed.

       [Commands will be moved here in Task 2]

       ## Commit & Merge

       Commands for committing and integrating completed work. These handle
       the git workflow: committing with quality gate enforcement, merging
       to main, and cleaning up worktrees.

       [Commands will be moved here by Plan 11-03]

       ## Session Management

       Commands for checking project state and recovering from interruptions.
       Use these when you need to understand current status or resume after
       a break.

       [Commands will be moved here by Plan 11-03]
       ```

    3. **Add Troubleshooting section placeholder** after Session Management:

       ```markdown
       ## Troubleshooting

       [Will be populated by Plan 11-03]
       ```

    4. **Preserve existing sections**: Keep "Workflow Shortcuts", "Argument
       Parsing", "Error Handling", and "Context Detection" sections at the end.

    DO NOT yet move command documentation into the new sections - that's
    Task 2. Just create the structure.
  </action>
  <verify>
    grep -q "## Quick Reference" src/vibraphone/commands/v.md
    grep -q "## Start Work" src/vibraphone/commands/v.md
    grep -q "## Run Quality" src/vibraphone/commands/v.md
    grep -q "## Commit & Merge" src/vibraphone/commands/v.md
    grep -q "## Session Management" src/vibraphone/commands/v.md
    grep -q "## Troubleshooting" src/vibraphone/commands/v.md
  </verify>
  <done>
    v.md has Quick Reference table with all 20 commands mapped to MCP tools.
    v.md has 4 workflow stage section headers with intro paragraphs.
    v.md has Troubleshooting section placeholder.
    Existing sections (Workflow Shortcuts, Argument Parsing, Error Handling,
    Context Detection) are preserved.
  </done>
</task>

<task type="auto">
  <name>Task 2: Expand dict-heavy command documentation</name>
  <files>src/vibraphone/commands/v.md</files>
  <action>
    Expand documentation for dict-heavy commands (init, configure-stack,
    import-plan) with full coverage examples. Move these commands into the
    appropriate workflow sections.

    **For each dict-heavy command, add:**

    1. **Typical usage** - Happy path example
    2. **With options** - Optional parameters demonstrated
    3. **Edge cases** - Multi-component, preview workflow
    4. **Common mistakes** - WRONG/RIGHT patterns for stringification

    **Placement:**
    - `/v init` -> Start Work section (first, since it sets up the project)
    - `/v configure-stack` -> Start Work section (after init)
    - `/v import-plan` -> Start Work section (after configure-stack)

    **Enhanced `/v init` documentation:**

    ```markdown
    ### /v init [--language LANG] [--name NAME] [--apply]

    Initialize vibraphone in the current project. Creates vibraphone.yaml
    and supporting configuration files.

    - `--language` / `-l`: Programming language (python, go, rust, node)
    - `--name` / `-n`: Project name (defaults to directory name)
    - `--apply`: Skip preview and write files immediately

    **MCP Tool**: `vibraphone_init_project`

    | Parameter    | Type    | Default | Required |
    | ------------ | ------- | ------- | -------- |
    | project_path | string  | null    | No       |
    | values       | object  | null    | No       |
    | preview      | boolean | true    | No       |

    **Typical usage (preview first):**

    User runs: `/v init`

    Claude calls:
    ```json
    {"preview": true}
    ```

    Claude shows preview, asks to confirm, then calls:
    ```json
    {"preview": false}
    ```

    **With language and name:**

    User runs: `/v init --language go --name myapp --apply`

    Claude calls:
    ```json
    {
      "preview": false,
      "values": {
        "language": "go",
        "project_name": "myapp"
      }
    }
    ```

    **Common mistake - WRONG stringification:**

    ```json
    // WRONG - values is a string, not an object
    {"values": "{\"language\": \"go\"}"}
    ```

    ```json
    // RIGHT - values is a native object
    {"values": {"language": "go"}}
    ```

    **Flow:**

    1. If no `--apply`: Call with `preview: true`, show preview, ask to confirm
    2. With `--apply` or after confirmation: Call with `preview: false`
    ```

    **Enhanced `/v configure-stack` documentation:**

    ```markdown
    ### /v configure-stack [--stitch PROJECT_ID]

    Configure test/lint/format commands for project components. Generates
    or updates the components section in vibraphone.yaml.

    - `--stitch`: Enable Stitch MCP integration with project ID

    **MCP Tool**: `vibraphone_configure_stack`

    | Parameter         | Type    | Default | Required |
    | ----------------- | ------- | ------- | -------- |
    | components        | object  | -       | Yes      |
    | stitch_project_id | string  | null    | No       |
    | preview           | boolean | true    | No       |

    **Typical usage (single Python component):**

    User runs: `/v configure-stack`

    Claude understands project and calls:
    ```json
    {
      "components": {
        "server": {
          "language": "python",
          "root": "./src",
          "test_command": "pytest",
          "lint_command": "ruff check .",
          "format_command": "ruff format ."
        }
      },
      "preview": true
    }
    ```

    **Multiple components (monorepo):**

    ```json
    {
      "components": {
        "backend": {
          "language": "python",
          "root": "./backend",
          "test_command": "pytest",
          "lint_command": "ruff check .",
          "format_command": "ruff format ."
        },
        "frontend": {
          "language": "node",
          "root": "./frontend",
          "test_command": "npm test",
          "lint_command": "npm run lint",
          "format_command": "npm run format"
        }
      },
      "preview": true
    }
    ```

    **With Stitch integration:**

    ```json
    {
      "components": {...},
      "stitch_project_id": "proj_abc123",
      "preview": false
    }
    ```

    **Common mistake - WRONG stringification:**

    ```json
    // WRONG - components is a string
    {"components": "{\"server\": {...}}"}
    ```

    ```json
    // RIGHT - components is a native object
    {"components": {"server": {...}}}
    ```

    **Flow:**

    1. Understand project structure (languages, test frameworks)
    2. Call with `preview: true`
    3. Show proposed config, ask to confirm
    4. Call with `preview: false`
    ```

    **Enhanced `/v import-plan` documentation:**

    ```markdown
    ### /v import-plan &lt;phase&gt;

    Import GSD phase plans into Beads tasks. Reads PLAN.md files from
    .planning/phases/ directory and creates corresponding Beads tasks.

    - `&lt;phase&gt;`: Phase number (required, e.g., 6 or 06)

    **MCP Tool**: `vibraphone_import_gsd_plan`

    | Parameter    | Type    | Default | Required |
    | ------------ | ------- | ------- | -------- |
    | phase_number | integer | -       | Yes      |
    | preview      | boolean | true    | No       |

    **Typical usage (preview first):**

    User runs: `/v import-plan 6`

    Claude calls:
    ```json
    {
      "phase_number": 6,
      "preview": true
    }
    ```

    Claude shows what would be imported, asks to confirm.

    **With apply (skip preview):**

    User runs: `/v import-plan 6 --apply`

    Claude calls:
    ```json
    {
      "phase_number": 6,
      "preview": false
    }
    ```

    **Edge case - Phase with no plans:**

    Error: `NoPlanFilesFound`
    Message: "No PLAN.md files found in phase directory"
    Suggested action: "Create PLAN.md files first"

    **Common mistake - WRONG parameter type:**

    ```json
    // WRONG - phase_number is a string
    {"phase_number": "6", "preview": true}
    ```

    ```json
    // RIGHT - phase_number is an integer
    {"phase_number": 6, "preview": true}
    ```

    **Flow:**

    1. Call with `preview: true`
    2. Show what would be imported (tasks, dependencies)
    3. Ask to confirm
    4. Call with `preview: false`
    ```

    After expanding these three commands, move them to the Start Work section
    (after the intro paragraph). Remove them from the old flat "Commands"
    section.

    Also move `/v check-prereqs` to Start Work section (between init and
    configure-stack) but keep its current documentation (no expansion needed).
  </action>
  <verify>
    grep -A5 "## Start Work" src/vibraphone/commands/v.md | grep -q "/v init"
    grep -A5 "## Start Work" src/vibraphone/commands/v.md | grep -q "/v configure-stack"
    grep -A5 "## Start Work" src/vibraphone/commands/v.md | grep -q "/v import-plan"
    grep -q "Common mistake - WRONG stringification" src/vibraphone/commands/v.md
  </verify>
  <done>
    /v init has expanded documentation with typical/with-options/edge/mistake
    examples.
    /v configure-stack has expanded documentation with multi-component example
    and stringification WRONG/RIGHT.
    /v import-plan has expanded documentation with type mistake example.
    All dict-heavy commands are in the Start Work section.
    WRONG/RIGHT stringification patterns are shown for each dict-heavy command.
  </done>
</task>

</tasks>

<verification>
- Quick Reference table exists with all 20 commands
- 4 workflow stage sections exist with intro paragraphs
- Dict-heavy commands have expanded inline JSON examples
- WRONG/RIGHT stringification patterns are documented
- Troubleshooting section placeholder exists
- Existing sections (Workflow Shortcuts, Argument Parsing, Error Handling,
  Context Detection) are preserved
</verification>

<success_criteria>
- v.md reorganized into 4 workflow stage groups
- Quick Reference table provides scannable command lookup
- Dict-heavy commands have comprehensive examples for copy-paste modification
- Stringification pitfall reinforced with WRONG/RIGHT patterns
</success_criteria>

<output>
After completion, create
`.planning/phases/11-command-documentation/11-01-SUMMARY.md`
</output>
