---
phase: 11-command-documentation
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - src/vibraphone/commands/v.md
autonomous: true
requirements: [SLASH-01, SLASH-02]
gap_closure: true

must_haves:
  truths:
    - "User runs /v init and Claude invokes vibraphone_init_project MCP tool"
    - "User runs /v list and Claude invokes vibraphone_list_tasks MCP tool"
    - "User runs /v test and Claude invokes vibraphone_run_tests MCP tool"
    - "All 18 commands route to correct MCP tools"
  artifacts:
    - path: "src/vibraphone/commands/v.md"
      provides: "Executable slash command with process routing"
      must_contain: ["<process>", "$ARGUMENTS", "mcp__vibraphone"]
  key_links:
    - from: "User /v command"
      to: "v.md <process>"
      via: "$ARGUMENTS parsing"
      pattern: "Parse.*subcommand"
    - from: "v.md <process>"
      to: "MCP tool"
      via: "Tool invocation"
      pattern: "mcp__vibraphone__\\w+"
---

<objective>
Add executable <process> section to v.md so slash commands actually invoke MCP tools.

Purpose: v.md is currently pure documentation. It needs a <process> section that
parses $ARGUMENTS and routes subcommands to the appropriate MCP tools.

Output: Working /v commands that invoke vibraphone MCP tools.
</objective>

<execution_context>
@/home/luke/.claude/get-shit-done/workflows/execute-plan.md
@/home/luke/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md

# Gap from UAT

The v.md slash command file is structured as pure reference documentation.
It lacks the <process> section that tells Claude Code what to do when the
user runs `/v init`.

**Root cause:** v.md only contains documentation - no executable instructions.

**Working examples:**
- `~/.claude/commands/gsd/debug.md` - Has proper `<process>` section
- `~/.claude/commands/gsd/progress.md` - Has proper `<process>` section

**Current v.md structure:**
- Frontmatter with name, description, allowed-tools
- Documentation sections (Quick Reference, Start Work, Run Quality, etc.)
- Argument parsing notes
- Error handling notes
- NO <process> section

**What's needed:**
1. Add <process> section before the documentation
2. Parse $ARGUMENTS to extract subcommand and flags
3. Route to appropriate MCP tool based on subcommand
4. Handle responses and present to user
</context>

<tasks>

<task type="auto">
  <name>Add executable process section to v.md</name>
  <files>src/vibraphone/commands/v.md</files>
  <action>
Add a `<process>` section immediately after the frontmatter (before the
documentation sections) that:

1. **Parses $ARGUMENTS** to extract:
   - Subcommand (init, list, next, start, test, lint, format, review,
     commit, merge, cleanup, complete, status, health, recover, cycle,
     finish, check-prereqs, configure-stack, import-plan)
   - Flags (--language, --name, --apply, --component, --files, --status,
     --plan, --stitch, --notes)
   - Positional arguments (task_id, message)

2. **Routes to MCP tools** using the mapping:
   - init -> mcp__vibraphone__init_project
   - check-prereqs -> mcp__vibraphone__check_prerequisites
   - configure-stack -> mcp__vibraphone__configure_stack
   - import-plan -> mcp__vibraphone__import_gsd_plan
   - list -> mcp__vibraphone__list_tasks
   - next -> mcp__vibraphone__next_ready
   - start -> mcp__vibraphone__start_task
   - test -> mcp__vibraphone__run_tests
   - lint -> mcp__vibraphone__run_lint
   - format -> mcp__vibraphone__run_format
   - review -> mcp__vibraphone__request_code_review
   - commit -> mcp__vibraphone__attempt_commit
   - merge -> mcp__vibraphone__merge_task
   - cleanup -> mcp__vibraphone__cleanup_task
   - complete -> mcp__vibraphone__complete_task
   - status -> mcp__vibraphone__recover_session
   - health -> mcp__vibraphone__health_check
   - recover -> mcp__vibraphone__recover_session
   - cycle -> Run test->lint->format->review in sequence
   - finish -> Run commit->merge->cleanup->complete in sequence

3. **Handles preview pattern** for init, configure-stack, import-plan:
   - If no --apply flag: call with preview=true, show result, ask to confirm
   - With --apply or after confirmation: call with preview=false

4. **Presents responses** to user:
   - Success: Show key information (task ID, status, counts)
   - Error: Show error message and suggested next_steps if available

5. **Preserves ALL existing documentation** - just insert the process section
   before "# Vibraphone Slash Commands" heading.

The process section should follow this pattern from working GSD commands:

```markdown
<process>

## 1. Parse Arguments

Parse $ARGUMENTS to extract subcommand and flags.

## 2. Route to MCP Tool

Based on subcommand, invoke the appropriate MCP tool with parsed parameters.

## 3. Handle Preview Commands

For init, configure-stack, import-plan: show preview and ask confirmation
unless --apply flag is set.

## 4. Handle Shortcuts

For cycle and finish: invoke multiple MCP tools in sequence.

## 5. Present Response

Show tool response to user with key information highlighted.

</process>
```

DO NOT remove or modify any existing documentation content. Only ADD the
process section.
  </action>
  <verify>
    grep -q "&lt;process&gt;" src/vibraphone/commands/v.md
    grep -q "mcp__vibraphone" src/vibraphone/commands/v.md
    grep -q "\$ARGUMENTS" src/vibraphone/commands/v.md
  </verify>
  <done>
    v.md contains a <process> section that routes subcommands to MCP tools.
    All existing documentation is preserved.
  </done>
</task>

</tasks>

<verification>
- v.md has <process> section after frontmatter
- Process section references $ARGUMENTS parsing
- Process section references MCP tool invocation (mcp__vibraphone)
- All existing documentation sections preserved (Quick Reference, Start Work,
  Run Quality, Commit & Merge, Session Management, Troubleshooting, Shortcuts)
</verification>

<success_criteria>
- User runs `/v init` and Claude invokes vibraphone_init_project MCP tool
- User runs `/v list` and Claude invokes vibraphone_list_tasks MCP tool
- User runs `/v test` and Claude invokes vibraphone_run_tests MCP tool
- All 18 commands have routing to their MCP tools
- Documentation is intact for reference
</success_criteria>

<output>
After completion, create `.planning/phases/11-command-documentation/11-05-SUMMARY.md`
</output>
