---
phase: 11-command-documentation
plan: 02
type: execute
wave: 2
depends_on: [11-01]
files_modified: [src/vibraphone/commands/v.md]
autonomous: true
requirements: [SLASH-01, SLASH-04]
user_setup: []

must_haves:
  truths:
    - User can understand Start Work commands with typical/edge/mistake examples
    - User can understand Run Quality commands with typical/edge/mistake examples
    - All core workflow commands have MCP tool names for auditability
  artifacts:
    - path: src/vibraphone/commands/v.md
      provides: Full coverage documentation for Start Work and Run Quality
      contains:
        - "### /v list"
        - "### /v next"
        - "### /v start"
        - "### /v test"
        - "### /v lint"
        - "### /v format"
        - "### /v review"
  key_links:
    - from: Start Work section
      to: Task management workflow
      via: list -> next -> start sequence
    - from: Run Quality section
      to: Quality gate tools
      via: test -> lint -> format -> review sequence
---

<objective>
Document Start Work and Run Quality commands with full coverage examples.

Purpose: Provide comprehensive documentation for the core workflow commands
that users invoke most frequently. Each command gets typical scenarios,
edge cases, and common mistakes to teach proper usage.

Output: Full coverage documentation for list, next, start, test, lint,
format, and review commands in their respective workflow sections.
</objective>

<execution_context>
@/home/luke/.claude/get-shit-done/workflows/execute-plan.md
@/home/luke/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-command-documentation/11-CONTEXT.md
@.planning/phases/11-command-documentation/11-RESEARCH.md
@src/vibraphone/commands/v.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Document Start Work commands with full coverage</name>
  <files>src/vibraphone/commands/v.md</files>
  <action>
    Expand documentation for list, next, and start commands with full coverage
    examples. Move them to the Start Work section.

    **Enhanced `/v list` documentation:**

    ```markdown
    ### /v list [--status STATUS] [--plan PLAN]

    List tasks from Beads. Shows task ID, title, status, and plan assignment.

    - `--status`: Filter by status (ready, in_progress, completed, blocked)
    - `--plan`: Filter by plan ID (e.g., "06-02")

    **MCP Tool**: `vibraphone_list_tasks`

    | Parameter | Type   | Default | Required |
    | --------- | ------ | ------- | -------- |
    | status    | string | null    | No       |
    | plan      | string | null    | No       |

    **Typical usage (all tasks):**

    User runs: `/v list`

    Claude calls:
    ```json
    {}
    ```

    **Filter by status:**

    User runs: `/v list --status ready`

    Claude calls:
    ```json
    {"status": "ready"}
    ```

    **Filter by plan:**

    User runs: `/v list --plan 06-02`

    Claude calls:
    ```json
    {"plan": "06-02"}
    ```

    **Combine filters:**

    User runs: `/v list --status in_progress --plan 06`

    Claude calls:
    ```json
    {
      "status": "in_progress",
      "plan": "06"
    }
    ```

    **Edge case - No tasks found:**

    Returns empty list with message "No tasks found matching criteria"
    ```

    **Enhanced `/v next` documentation:**

    ```markdown
    ### /v next

    Get the next ready task using critical path analysis. Considers
    dependencies, priorities, and blocking relationships.

    **MCP Tool**: `vibraphone_next_ready`

    No parameters required.

    **Typical usage:**

    User runs: `/v next`

    Claude calls:
    ```json
    {}
    ```

    **Response includes:**

    - Task ID (e.g., "bd-abc123")
    - Title and description
    - Why this task was selected (dependencies met, high priority, etc.)
    - Suggested next action

    **Edge case - No ready tasks:**

    Response: "No ready tasks found"
    Suggested actions:
    - Check if blocked tasks have unresolved dependencies
    - Complete in-progress tasks first
    - Import new plans with `/v import-plan`
    ```

    **Enhanced `/v start` documentation:**

    ```markdown
    ### /v start &lt;task_id&gt;

    Start a task in an isolated git worktree. Creates a branch, sets up
    the worktree, and begins a session.

    - `&lt;task_id&gt;`: Task ID from Beads (required, e.g., bd-abc123)

    **MCP Tool**: `vibraphone_start_task`

    | Parameter | Type   | Default | Required |
    | --------- | ------ | ------- | -------- |
    | task_id   | string | -       | Yes      |

    **Typical usage:**

    User runs: `/v start bd-abc123`

    Claude calls:
    ```json
    {"task_id": "bd-abc123"}
    ```

    **Response includes:**

    - Worktree path (e.g., ~/.vibraphone/worktrees/bd-abc123/)
    - Branch name
    - Session ID

    **Edge case - Task already in progress:**

    Error: `BranchAlreadyExists`
    Message: "Branch 'task/bd-abc123' already exists"
    Suggested action: "Task may already be in progress. Check existing
    worktrees with `git worktree list`"

    **Edge case - Blocked task:**

    Error: `CannotStartBlockedTask`
    Message: "Task has incomplete dependencies"
    Suggested action: Lists blocking tasks to complete first

    **Common mistake - Wrong ID format:**

    ```text
    # WRONG - Using numeric ID
    /v start 123

    # RIGHT - Use full task ID from br output
    /v start bd-abc123
    ```
    ```

    Move these three commands to the Start Work section (after the intro
    paragraph and after check-prereqs). Remove them from any old flat
    Commands section.
  </action>
  <verify>
    grep -q "### /v list" src/vibraphone/commands/v.md
    grep -q "### /v next" src/vibraphone/commands/v.md
    grep -q "### /v start" src/vibraphone/commands/v.md
    grep -q "Typical usage" src/vibraphone/commands/v.md
    grep -q "Edge case" src/vibraphone/commands/v.md
    grep -q "Common mistake" src/vibraphone/commands/v.md
  </verify>
  <done>
    /v list has full coverage with typical/filter/combine/edge examples.
    /v next has full coverage with typical and no-tasks edge case.
    /v start has full coverage with typical/edge/error examples.
    All three commands are in Start Work section.
  </done>
</task>

<task type="auto">
  <name>Task 2: Document Run Quality commands with full coverage</name>
  <files>src/vibraphone/commands/v.md</files>
  <action>
    Expand documentation for test, lint, format, and review commands with
    full coverage examples. Move them to the Run Quality section.

    **Enhanced `/v test` documentation:**

    ```markdown
    ### /v test [--component NAME]

    Run tests in the current worktree context. Uses the test_command from
    vibraphone.yaml for the specified component.

    - `--component`: Component name to test (optional, defaults to all)

    **MCP Tool**: `vibraphone_run_tests`

    | Parameter | Type   | Default | Required |
    | --------- | ------ | ------- | -------- |
    | component | string | null    | No       |

    **Typical usage (all components):**

    User runs: `/v test`

    Claude calls:
    ```json
    {}
    ```

    **Specific component:**

    User runs: `/v test --component server`

    Claude calls:
    ```json
    {"component": "server"}
    ```

    **Success response:**

    - Status: PASSED
    - Test count and pass rate

    **Failure response:**

    - Status: FAILED
    - Failure details with file/line
    - Suggested fixes

    **Edge case - Circuit breaker triggered:**

    After max_test_attempts exceeded:
    - Status: ESCALATED
    - Message: "Test failures persist after N attempts"
    - Suggested action: Manual intervention required
    ```

    **Enhanced `/v lint` documentation:**

    ```markdown
    ### /v lint [--component NAME]

    Run linter in the current worktree context. Uses the lint_command from
    vibraphone.yaml.

    - `--component`: Component name to lint (optional, defaults to all)

    **MCP Tool**: `vibraphone_run_lint`

    | Parameter | Type   | Default | Required |
    | --------- | ------ | ------- | -------- |
    | component | string | null    | No       |

    **Typical usage:**

    User runs: `/v lint`

    Claude calls:
    ```json
    {}
    ```

    **Success response:**

    - Status: PASSED
    - No lint errors found

    **Failure response:**

    - Status: FAILED
    - Violation list with file/line/rule
    - Many violations are auto-fixable

    **Common workflow:**

    ```text
    /v lint          # See violations
    # Fix or auto-fix issues
    /v lint          # Re-run until PASSED
    ```
    ```

    **Enhanced `/v format` documentation:**

    ```markdown
    ### /v format [--component NAME]

    Run formatter in the current worktree context. Uses the format_command
    from vibraphone.yaml.

    - `--component`: Component name to format (optional, defaults to all)

    **MCP Tool**: `vibraphone_run_format`

    | Parameter | Type   | Default | Required |
    | --------- | ------ | ------- | -------- |
    | component | string | null    | No       |

    **Typical usage:**

    User runs: `/v format`

    Claude calls:
    ```json
    {}
    ```

    **Response includes:**

    - Status: PASSED (formatting applied)
    - Files modified count

    **Note:** Formatters typically modify files in place. The quality gate
    checks that the formatter ran successfully, not that files were unchanged.
    ```

    **Enhanced `/v review` documentation:**

    ```markdown
    ### /v review [--files FILE1,FILE2,...]

    Request LLM-powered code review. Uses instructor + OpenRouter to
    analyze code changes and provide feedback.

    - `--files`: Comma-separated list of files to review (optional)

    **MCP Tool**: `vibraphone_request_code_review`

    | Parameter | Type          | Default | Required |
    | --------- | ------------- | ------- | -------- |
    | task_id   | string        | null    | No       |
    | files     | array[string] | null    | No       |

    **Typical usage (all changed files):**

    User runs: `/v review`

    Claude calls:
    ```json
    {}
    ```

    **Specific files:**

    User runs: `/v review --files src/main.py,src/utils.py`

    Claude calls:
    ```json
    {
      "files": ["src/main.py", "src/utils.py"]
    }
    ```

    **Common mistake - WRONG array format:**

    ```json
    // WRONG - files is a string
    {"files": "src/main.py,src/utils.py"}
    ```

    ```json
    // RIGHT - files is an array
    {"files": ["src/main.py", "src/utils.py"]}
    ```

    **Success response:**

    - Status: APPROVED
    - Review summary
    - No blocking issues

    **Rejection response:**

    - Status: REJECTED
    - Issues list with severity and location
    - Suggested fixes

    **Edge case - Missing API key:**

    Error: `MissingAPIKeyError`
    Message: "REVIEWER_API_KEY not set"
    Suggested action: "Set REVIEWER_API_KEY in environment or .env file"

    **Edge case - Circuit breaker:**

    After max_review_attempts exceeded:
    - Status: ESCALATED
    - Message: "Review not approved after N attempts"
    - Suggested action: Manual review required
    ```

    Move these four commands to the Run Quality section (after the intro
    paragraph). Remove them from any old flat Commands section.
  </action>
  <verify>
    grep -q "### /v test" src/vibraphone/commands/v.md
    grep -q "### /v lint" src/vibraphone/commands/v.md
    grep -q "### /v format" src/vibraphone/commands/v.md
    grep -q "### /v review" src/vibraphone/commands/v.md
    grep -q "Circuit breaker" src/vibraphone/commands/v.md
    grep -q "Common mistake - WRONG array format" src/vibraphone/commands/v.md
  </verify>
  <done>
    /v test has full coverage with typical/component/edge/circuit-breaker.
    /v lint has full coverage with typical/failure/workflow examples.
    /v format has full coverage with typical response documentation.
    /v review has full coverage with typical/files/mistake/edge examples.
    All four commands are in Run Quality section.
    Circuit breaker escalation is documented for quality gates.
  </done>
</task>

</tasks>

<verification>
- Start Work section contains: init, check-prereqs, configure-stack,
  import-plan, list, next, start
- Run Quality section contains: test, lint, format, review
- Each command has Typical usage, Edge cases, Common mistakes sections
- MCP tool names are shown for each command
- Circuit breaker escalation is documented
</verification>

<success_criteria>
- All Start Work commands have full coverage documentation
- All Run Quality commands have full coverage documentation
- Quality gate failure handling is documented
- MCP tool auditability is maintained
</success_criteria>

<output>
After completion, create
`.planning/phases/11-command-documentation/11-02-SUMMARY.md`
</output>
