---
phase: 05-quality-gate-tools
plan: 04
type: execute
wave: 4
depends_on: [05-03]
files_modified:
  - tests/test_command_runner.py
  - tests/test_circuit_breaker.py
  - tests/test_quality_state.py
  - tests/test_code_reviewer.py
  - tests/test_quality_gate_tools.py
  - tests/test_phase5_success.py
autonomous: true
requirements: [QUAL-01, QUAL-02, QUAL-03, QUAL-04, QUAL-05, QUAL-06]
user_setup: []

must_haves:
  truths:
    - "Unit tests cover command runner with mocked subprocess execution"
    - "Unit tests cover circuit breaker escalation behavior"
    - "Unit tests cover quality state atomic persistence"
    - "Unit tests cover code reviewer with mocked LLM client"
    - "Unit tests cover all 5 quality gate tools with mocked dependencies"
    - "Success criteria tests verify all 6 ROADMAP requirements"
    - "All 104+ tests pass (existing + new)"
  artifacts:
    - path: "tests/test_command_runner.py"
      provides: "Unit tests for command_runner module"
      min_lines: 50
    - path: "tests/test_circuit_breaker.py"
      provides: "Unit tests for circuit_breaker module"
      min_lines: 40
    - path: "tests/test_quality_state.py"
      provides: "Unit tests for quality_state module"
      min_lines: 50
    - path: "tests/test_code_reviewer.py"
      provides: "Unit tests for code_reviewer module"
      min_lines: 40
    - path: "tests/test_quality_gate_tools.py"
      provides: "Unit tests for all 5 MCP tools"
      min_lines: 100
    - path: "tests/test_phase5_success.py"
      provides: "Success criteria verification for QUAL-01 through QUAL-06"
      min_lines: 80
  key_links:
    - from: "tests/test_quality_gate_tools.py"
      to: "tools/quality_gate_tools.py"
      via: ".fn attribute for FastMCP tool access"
      pattern: "\\.fn\\(\\)"
    - from: "tests/test_phase5_success.py"
      to: "ROADMAP.md"
      via: "requirement IDs in test names"
      pattern: "test_QUAL_\\d+"
---

<objective>
Create comprehensive unit tests for all Phase 5 modules and success criteria verification tests that validate all 6 ROADMAP requirements.

Purpose: Tests ensure quality gate tools work correctly and provide regression protection. Success criteria tests prove Phase 5 is complete by verifying each ROADMAP requirement.

Output: 6 test files covering all new modules and Phase 5 success criteria.
</objective>

<execution_context>
@/home/luke/.claude/get-shit-done/workflows/execute-plan.md
@/home/luke/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-quality-gate-tools/05-CONTEXT.md

# Existing test patterns
@tests/test_session.py
@tests/test_worktree_tools.py
@tests/test_phase4_success.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create unit tests for command_runner module</name>
  <files>tests/test_command_runner.py</files>
  <action>
Create unit tests for command_runner.py following patterns from test_session.py.

Test class: TestCommandRunner

Test cases:
1. test_get_command_returns_default - get_command("test", {}) returns "just test"
2. test_get_command_uses_config_override - get_command("test", {"quality_gate": {"commands": {"test": "pytest"}}}) returns "pytest"
3. test_get_command_with_component - get_command("test", {}, component="server") returns "just test-server"
4. test_get_command_component_no_justfile - get_command with component but non-just command leaves it unchanged
5. test_run_command_success - mock asyncio.create_subprocess_exec, return 0 with stdout
6. test_run_command_failure - mock return non-zero, check returncode in result
7. test_run_command_with_cwd - verify cwd parameter passed to subprocess

Use pytest-mock for mocking asyncio.create_subprocess_exec.
Use monkeypatch for config mocking.

Follow the pattern from test_worktree_ops.py for async subprocess mocking.
</action>
  <verify>python -m pytest tests/test_command_runner.py -v</verify>
  <done>All tests in test_command_runner.py pass. Tests cover get_command defaults, overrides, component suffixing, and run_command async execution.</done>
</task>

<task type="auto">
  <name>Task 2: Create unit tests for circuit_breaker module</name>
  <files>tests/test_circuit_breaker.py</files>
  <action>
Create unit tests for circuit_breaker.py.

Test class: TestCircuitBreaker

Test cases:
1. test_check_returns_none_when_not_tripped - CircuitBreaker(5, "tests").check(3) returns None
2. test_check_returns_escalation_when_tripped - CircuitBreaker(5, "tests").check(5) returns escalation dict
3. test_check_returns_escalation_when_exceeded - CircuitBreaker(5, "tests").check(6) returns escalation dict
4. test_none_max_attempts_never_trips - CircuitBreaker(None, "lint").check(100) returns None
5. test_escalation_response_format - Verify dict has status, error_type, message, human_actions, next_steps
6. test_escalation_includes_all_human_actions - human_actions list has all 3 options
7. test_is_tripped_method - is_tripped() returns bool correctly
</action>
  <verify>python -m pytest tests/test_circuit_breaker.py -v</verify>
  <done>All tests in test_circuit_breaker.py pass. Tests cover tripped/not tripped states, None max_attempts, and escalation response format.</done>
</task>

<task type="auto">
  <name>Task 3: Create unit tests for quality_state module</name>
  <files>tests/test_quality_state.py</files>
  <action>
Create unit tests for quality_state.py following patterns from test_session.py.

Test class: TestQualityState

Test cases:
1. test_quality_gate_state_defaults - New state has None values and 0 counters
2. test_quality_gate_state_model_dump_json - Serialization works correctly
3. test_manager_load_returns_none_for_missing_file - SessionManager.load() returns None
4. test_manager_save_and_load - Save state, load returns same values
5. test_manager_atomic_write - Verify temp file pattern used (mock tempfile)
6. test_manager_clear - clear() deletes file
7. test_state_with_review_data - State with last_review_status, diff_hash, issues
8. test_get_quality_state_manager - Helper function returns manager

Use tmp_path fixture for file operations.
Use monkeypatch for cwd mocking if needed.
</action>
  <verify>python -m pytest tests/test_quality_state.py -v</verify>
  <done>All tests in test_quality_state.py pass. Tests cover state model, manager load/save/clear, atomic write pattern, and helper function.</done>
</task>

<task type="auto">
  <name>Task 4: Create unit tests for code_reviewer module</name>
  <files>tests/test_code_reviewer.py</files>
  <action>
Create unit tests for code_reviewer.py with mocked LLM client.

Test class: TestCodeReviewer

Test cases:
1. test_review_issue_model - ReviewIssue creates with all fields
2. test_review_result_model - ReviewResult creates with issues list and summary
3. test_code_reviewer_init - CodeReviewer initializes with model name
4. test_code_reviewer_missing_api_key - Clear error when REVIEWER_API_KEY not set
5. test_code_reviewer_review_returns_result - Mock instructor client, verify ReviewResult returned
6. test_code_reviewer_includes_previous_issues - Previous issues passed to prompt

For mocking instructor:
- Mock instructor.patch() to return mock client
- Mock client.chat.completions.create() to return ReviewResult
- Use monkeypatch.delenv for API key test

Note: These tests verify the code path, not actual LLM calls.
</action>
  <verify>python -m pytest tests/test_code_reviewer.py -v</verify>
  <done>All tests in test_code_reviewer.py pass. Tests cover model creation, initialization, missing API key error, and review method with mocked client.</done>
</task>

<task type="auto">
  <name>Task 5: Create unit tests for quality_gate_tools module</name>
  <files>tests/test_quality_gate_tools.py</files>
  <action>
Create unit tests for all 5 quality gate tools following patterns from test_worktree_tools.py.

Test class: TestQualityGateTools

Test cases for run_tests:
1. test_run_tests_pass - Mock run_command returns 0, status is "pass"
2. test_run_tests_fail - Mock run_command returns 1, status is "fail"
3. test_run_tests_with_component - Component passed to get_command
4. test_run_tests_circuit_breaker_tripped - High attempt count triggers escalation

Test cases for run_lint:
5. test_run_lint_pass - Mock run_command returns 0
6. test_run_lint_fail - Mock run_command returns 1

Test cases for run_format:
7. test_run_format_success - Mock run_command returns 0

Test cases for request_code_review:
8. test_request_code_review_approved - Mock reviewer returns only warnings
9. test_request_code_review_rejected - Mock reviewer returns errors
10. test_request_code_review_blocks_dangerous_files - .env file not staged
11. test_request_code_review_circuit_breaker - Escalation after max attempts

Test cases for attempt_commit:
12. test_attempt_commit_no_review - Fails without approved review
13. test_attempt_commit_diff_mismatch - Fails when diff hash changed
14. test_attempt_commit_quality_check_fails - Fails when just check fails
15. test_attempt_commit_success - All checks pass, commit executes

Mocking patterns:
- Patch at import location: vibraphone.tools.quality_gate_tools.run_command
- Use .fn attribute to call tool directly
- Mock QualityStateManager, CodeReviewer as needed
</action>
  <verify>python -m pytest tests/test_quality_gate_tools.py -v</verify>
  <done>All tests in test_quality_gate_tools.py pass. Tests cover all 5 tools with mocked dependencies, circuit breakers, dangerous file blocking, and commit enforcement.</done>
</task>

<task type="auto">
  <name>Task 6: Create Phase 5 success criteria verification tests</name>
  <files>tests/test_phase5_success.py</files>
  <action>
Create success criteria tests that verify all 6 ROADMAP requirements for Phase 5.

Follow the pattern from test_phase4_success.py - each test verifies a ROADMAP requirement.

Test class: TestPhase5SuccessCriteria

Test cases (named by requirement ID):

QUAL-01: Agent can run tests with circuit breaker
```python
def test_QUAL_01_run_tests_with_circuit_breaker(self, mocker):
    # Verify run_tests tool exists
    # Verify circuit_breaker.check is called on failure
    # Verify escalation response when tripped
```

QUAL-02: Agent can run linter
```python
def test_QUAL_02_run_lint(self, mocker):
    # Verify run_lint tool exists
    # Verify it returns structured output
```

QUAL-03: Agent can run formatter
```python
def test_QUAL_03_run_format(self, mocker):
    # Verify run_format tool exists
    # Verify it returns structured output
```

QUAL-04: Agent can request LLM-powered code review with staging
```python
def test_QUAL_04_request_code_review(self, mocker):
    # Verify request_code_review tool exists
    # Verify dangerous files blocked
    # Verify APPROVED/REJECTED based on issue severity
```

QUAL-05: Agent can commit only after approved review
```python
def test_QUAL_05_attempt_commit_requires_review(self, mocker):
    # Verify attempt_commit fails without APPROVED status
    # Verify attempt_commit fails when diff hash mismatch
    # Verify attempt_commit succeeds with APPROVED + matching hash
```

QUAL-06: Circuit breakers escalate after configurable max attempts
```python
def test_QUAL_06_circuit_breaker_escalation(self, mocker):
    # Verify circuit breaker escalation response format
    # Verify human_actions list present
    # Verify config-driven thresholds
```
</action>
  <verify>python -m pytest tests/test_phase5_success.py -v</verify>
  <done>All 6 success criteria tests pass. Each test verifies a ROADMAP requirement by name (QUAL-01 through QUAL-06).</done>
</task>

</tasks>

<verification>
- All new test files exist and import correctly
- All tests pass: python -m pytest tests/ -v
- Total test count is 104+ (existing + ~40 new)
- Each ROADMAP requirement has a corresponding success criteria test
</verification>

<success_criteria>
1. test_command_runner.py has tests for get_command and run_command
2. test_circuit_breaker.py has tests for trip detection and escalation format
3. test_quality_state.py has tests for state model and manager
4. test_code_reviewer.py has tests for models and reviewer
5. test_quality_gate_tools.py has tests for all 5 tools
6. test_phase5_success.py has tests for QUAL-01 through QUAL-06
7. All 104+ tests pass with pytest
</success_criteria>

<output>
After completion, create `.planning/phases/05-quality-gate-tools/05-04-SUMMARY.md`
</output>
