---
phase: 09-testing-documentation
plan: 05
type: execute
wave: 2
depends_on:
  - 09-04
files_modified:
  - pyproject.toml
  - mkdocs.yml
  - docs/index.md
  - docs/configuration.md
  - docs/tools/task-tools.md
  - docs/tools/quality-tools.md
  - docs/tools/worktree-tools.md
  - docs/tools/scaffold-tools.md
  - docs/tools/bridge-tools.md
  - docs/architecture.md
  - docs/errors.md
autonomous: true
requirements:
  - DOC-02
  - DOC-03

must_haves:
  truths:
    - "MkDocs with mkdocstrings builds successfully"
    - "API reference documents all MCP tools with parameters"
    - "Architecture document explains internal design"
    - "Error reference documents all error types"
    - "mkdocs serve works for local preview"
  artifacts:
    - path: "mkdocs.yml"
      provides: "MkDocs configuration"
      contains: "mkdocstrings"
    - path: "docs/tools/task-tools.md"
      provides: "Task tools API reference"
      min_lines: 30
    - path: "docs/architecture.md"
      provides: "Architecture documentation"
      min_lines: 50
    - path: "docs/errors.md"
      provides: "Error reference"
      contains: "TaskError"
  key_links:
    - from: "docs/tools/*.md"
      to: "src/vibraphone/tools/*.py"
      via: "mkdocstrings ::: syntax"
      pattern: "::: vibraphone"
---

<objective>
Create MkDocs documentation site with API reference and architecture docs.

Purpose: Set up MkDocs with mkdocstrings for hybrid documentation where API reference is auto-generated from Google-style docstrings while guides remain hand-written markdown. Per CONTEXT.md: "Per-category files" structure, "Hybrid" source approach.

Output: MkDocs site with tool API reference, architecture docs, and error reference.
</objective>

<execution_context>
@/home/luke/.claude/get-shit-done/workflows/execute-plan.md
@/home/luke/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-testing-documentation/09-CONTEXT.md
@.planning/phases/09-testing-documentation/09-RESEARCH.md

# Source files for docstring reference
@src/vibraphone/tools/task_tools.py
@src/vibraphone/tools/quality_gate_tools.py
@src/vibraphone/utils/errors.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add MkDocs dependencies and create mkdocs.yml</name>
  <files>pyproject.toml, mkdocs.yml</files>
  <action>
Add MkDocs documentation dependencies and create configuration.

1. Add dependencies:
```bash
uv add --group dev mkdocs mkdocstrings mkdocs-material
```

2. Create mkdocs.yml:
```yaml
site_name: Vibraphone
site_description: MCP server for AI coding agents with quality gates
repo_url: https://github.com/lukemcguire/vibraphone
repo_name: lukemcguire/vibraphone

theme:
  name: material
  palette:
    - scheme: default
      primary: indigo
      accent: indigo
      toggle:
        icon: material/brightness-7
        name: Switch to dark mode
    - scheme: slate
      primary: indigo
      accent: indigo
      toggle:
        icon: material/brightness-4
        name: Switch to light mode

nav:
  - Home: index.md
  - Configuration: configuration.md
  - Architecture: architecture.md
  - Errors: errors.md
  - Tools:
      - Task Tools: tools/task-tools.md
      - Quality Tools: tools/quality-tools.md
      - Worktree Tools: tools/worktree-tools.md
      - Scaffold Tools: tools/scaffold-tools.md
      - Bridge Tools: tools/bridge-tools.md

plugins:
  - search
  - mkdocstrings:
      handlers:
        python:
          options:
            docstring_style: google
            show_source: false
            show_root_heading: true
            show_root_full_path: false
            merge_init_into_class: true

markdown_extensions:
  - pymdownx.highlight
  - pymdownx.superfences
```

3. Create docs/ directory structure:
```bash
mkdir -p docs/tools
```
  </action>
  <verify>cd /home/luke/workspace/github.com/lukemcguire/vibraphone && test -f mkdocs.yml && grep -q "mkdocstrings" mkdocs.yml && echo "OK"</verify>
  <done>mkdocs.yml exists with mkdocstrings plugin; docs/ directory created</done>
</task>

<task type="auto">
  <name>Task 2: Create docs index and configuration pages</name>
  <files>docs/index.md, docs/configuration.md</files>
  <action>
Create documentation landing page and configuration reference.

1. Create docs/index.md:
```markdown
# Vibraphone

Vibraphone is an MCP server that enforces quality gates for AI coding agents.

## Why Vibraphone?

AI coding agents are powerful but unsafe. They skip tests, ignore lint failures, and commit without review. Vibraphone enforces quality gates through tooling - not prompting.

## Features

- **Quality Gates**: Tests, lint, format, and LLM-powered code review before any commit
- **Worktree Isolation**: Each task gets its own git worktree
- **Session Recovery**: Resume interrupted work
- **Circuit Breakers**: Automatic escalation on repeated failures

## Quick Links

- [Installation & Quickstart](https://github.com/lukemcguire/vibraphone#quickstart)
- [Configuration](configuration.md)
- [Tool Reference](tools/task-tools.md)
- [Architecture](architecture.md)
```

2. Create docs/configuration.md with full configuration reference:
```markdown
# Configuration

Vibraphone is configured via `vibraphone.yaml` in your project root.

## Full Configuration Example

\`\`\`yaml
worktrees_path: ~/.vibraphone/worktrees/

circuit_breakers:
  tests:
    max_attempts: 3
  lint:
    max_attempts: 3
  format:
    max_attempts: 3
  review:
    max_attempts: 5

review:
  model: openai/gpt-4o-mini

quality_gate:
  commands:
    test: pytest
    lint: ruff check
    format: ruff format
    check: ruff check
    test_component: pytest tests/test_{component}.py
    lint_component: ruff check src/{component}/
```

## Configuration Fields

### worktrees_path

Path where git worktrees are created. Defaults to `~/.vibraphone/worktrees/`.

### circuit_breakers

Circuit breaker configuration for each quality gate tool.

| Field | Default | Description |
|-------|---------|-------------|
| `max_attempts` | 3 | Max failures before escalation. Set to `null` to disable. |

### review

Code review configuration.

| Field | Default | Description |
|-------|---------|-------------|
| `model` | openai/gpt-4o-mini | OpenRouter model for code review |

### quality_gate.commands

Command templates for quality gate tools.

| Field | Default | Description |
|-------|---------|-------------|
| `test` | pytest | Test command |
| `lint` | ruff check | Lint command |
| `format` | ruff format | Format command |
| `check` | ruff check | Pre-commit check command |
| `test_component` | (none) | Per-component test command pattern |
| `lint_component` | (none) | Per-component lint command pattern |
```
  </action>
  <verify>cd /home/luke/workspace/github.com/lukemcguire/vibraphone && test -f docs/index.md && test -f docs/configuration.md && wc -l docs/configuration.md</verify>
  <done>docs/index.md and docs/configuration.md exist; configuration.md has field reference tables</done>
</task>

<task type="auto">
  <name>Task 3: Create tool API reference pages with mkdocstrings</name>
  <files>docs/tools/task-tools.md, docs/tools/quality-tools.md, docs/tools/worktree-tools.md, docs/tools/scaffold-tools.md, docs/tools/bridge-tools.md</files>
  <action>
Create API reference pages using mkdocstrings ::: syntax.

Per CONTEXT.md: "Per-category files" structure, "Examples + API" content.

1. Create docs/tools/task-tools.md:
```markdown
# Task Tools

Tools for managing tasks via beads_rust (br/bv CLI).

## list_tasks

::: vibraphone.tools.task_tools.list_tasks
    options:
      docstring_style: google

## next_ready

::: vibraphone.tools.task_tools.next_ready
    options:
      docstring_style: google

## health_check

::: vibraphone.tools.task_tools.health_check
    options:
      docstring_style: google

## complete_task

::: vibraphone.tools.task_tools.complete_task
    options:
      docstring_style: google

## abandon_task

::: vibraphone.tools.task_tools.abandon_task
    options:
      docstring_style: google

## get_task_context

::: vibraphone.tools.task_tools.get_task_context
    options:
      docstring_style: google
```

2. Create docs/tools/quality-tools.md:
```markdown
# Quality Gate Tools

Tools for enforcing quality gates before commits.

## run_tests

::: vibraphone.tools.quality_gate_tools.run_tests
    options:
      docstring_style: google

## run_lint

::: vibraphone.tools.quality_gate_tools.run_lint
    options:
      docstring_style: google

## run_format

::: vibraphone.tools.quality_gate_tools.run_format
    options:
      docstring_style: google

## request_code_review

::: vibraphone.tools.quality_gate_tools.request_code_review
    options:
      docstring_style: google

## attempt_commit

::: vibraphone.tools.quality_gate_tools.attempt_commit
    options:
      docstring_style: google
```

3. Create docs/tools/worktree-tools.md for start_task, merge_task, cleanup_task, recover_session

4. Create docs/tools/scaffold-tools.md for init_project, check_prerequisites

5. Create docs/tools/bridge-tools.md for import_gsd_plan, configure_stack
  </action>
  <verify>cd /home/luke/workspace/github.com/lukemcguire/vibraphone && ls docs/tools/*.md | wc -l</verify>
  <done>5 tool API reference files exist in docs/tools/; each uses mkdocstrings ::: syntax</done>
</task>

<task type="auto">
  <name>Task 4: Create architecture and error documentation</name>
  <files>docs/architecture.md, docs/errors.md</files>
  <action>
Create architecture overview and error reference documentation.

1. Create docs/architecture.md:
```markdown
# Architecture

Vibraphone is an MCP (Model Context Protocol) server that provides tools for AI coding agents.

## System Overview

\`\`\`
┌─────────────────────────────────────────────────────────────┐
│                     MCP Client (Claude)                      │
│                    (calls vibraphone tools)                  │
└──────────────────────────┬──────────────────────────────────┘
                           │ stdio transport
                           ▼
┌─────────────────────────────────────────────────────────────┐
│                   Vibraphone MCP Server                      │
├─────────────────────────────────────────────────────────────┤
│  Task Tools     │  Quality Gates  │  Worktree Tools         │
│  (list_tasks,   │  (run_tests,    │  (start_task,           │
│   next_ready,   │   run_lint,     │   merge_task,           │
│   complete...)  │   review...)    │   cleanup...)           │
└────────┬────────┴────────┬────────┴────────┬────────────────┘
         │                 │                 │
         ▼                 ▼                 ▼
┌────────────────┐ ┌────────────────┐ ┌────────────────────────┐
│ beads_rust CLI │ │ git / pytest   │ │ git worktree           │
│ (br, bv)       │ │ ruff / LLM     │ │ .vibraphone/session    │
└────────────────┘ └────────────────┘ └────────────────────────┘
\`\`\`

## Key Components

### Configuration (config.py)

- Discovers `vibraphone.yaml` by walking up from current directory
- Validates configuration with Pydantic
- Returns sensible defaults when config missing

### Session Management (utils/session.py)

- Tracks active worktree and task
- Persists to `.vibraphone/session.json`
- Enables session recovery on server restart

### Quality Gates (tools/quality_gate_tools.py)

- Execute test/lint/format commands
- Circuit breakers for repeated failures
- LLM-powered code review via instructor + OpenRouter
- Review-before-commit enforcement

### Worktree Isolation (utils/worktree_ops.py)

- Creates isolated git worktrees per task
- Manages branch lifecycle (create, rebase, delete)
- Safety guards for uncommitted changes

## Data Flow

1. Agent calls `start_task` -> worktree created, session saved
2. Agent works in worktree, calls quality gates
3. Agent calls `request_code_review` -> LLM reviews diff
4. Agent calls `attempt_commit` -> commit only if APPROVED
5. Agent calls `merge_task` -> rebase onto main
6. Agent calls `cleanup_task` -> worktree removed

## File Locations

| Path | Purpose |
|------|---------|
| `vibraphone.yaml` | Project configuration |
| `.vibraphone/session.json` | Active session state |
| `.vibraphone/tasks/{id}/state.json` | Per-task quality gate state |
| `~/.vibraphone/worktrees/` | Worktree storage |
```

2. Create docs/errors.md:
```markdown
# Error Reference

Vibraphone uses structured error responses for consistent handling.

## Error Response Format

All errors follow this structure:

\`\`\`python
{
    "status": "error",
    "error_type": "ErrorTypeName",
    "message": "Human-readable error message",
    "suggested_action": "What to do next"
}
\`\`\`

## Error Types

### TaskError

Base error for task-related operations.

| error_type | When Raised |
|------------|-------------|
| `TaskNotFound` | Task ID doesn't exist in beads |
| `TaskBlocked` | Cannot complete blocked task |
| `CliError` | br/bv command failed |

### WorktreeError

Errors for worktree operations.

| error_type | When Raised |
|------------|-------------|
| `WorktreeExists` | Worktree already exists for task |
| `WorktreeNotFound` | No worktree for task ID |
| `UncommittedChanges` | Cannot proceed with uncommitted changes |

### RebaseError

Errors during rebase operations.

| error_type | When Raised |
|------------|-------------|
| `RebaseConflict` | Merge conflicts during rebase |
| `RebaseFailed` | Generic rebase failure |

### CircuitBreakerTripped

When quality gate exceeds max attempts.

\`\`\`python
{
    "status": "ESCALATED",
    "error_type": "CircuitBreakerTripped",
    "message": "Tests failed 3 times (max: 3)",
    "human_actions": [
        "Run tests locally to diagnose failure",
        "Fix the failing tests and reset: cleanup_task",
        "Abandon this approach: abandon_task"
    ]
}
\`\`\`

## Handling Errors

Agents should:

1. Check `error_type` for categorization
2. Read `message` for specifics
3. Follow `suggested_action` or `human_actions`
```
  </action>
  <verify>cd /home/luke/workspace/github.com/lukemcguire/vibraphone && test -f docs/architecture.md && test -f docs/errors.md && grep -q "CircuitBreakerTripped" docs/errors.md && echo "OK"</verify>
  <done>docs/architecture.md and docs/errors.md exist; architecture has system diagram; errors has all error types</done>
</task>

<task type="auto">
  <name>Task 5: Verify MkDocs build works</name>
  <files>mkdocs.yml</files>
  <action>
Verify the MkDocs documentation builds successfully.

1. Build docs:
```bash
uv run mkdocs build
```

2. Check site/ directory was created:
```bash
ls -la site/
```

3. Verify key pages exist:
```bash
ls site/tools/
ls site/index.html
```

If build fails with module not found errors:
- Check mkdocstrings handler path configuration
- Ensure src/vibraphone is importable
- Run from project root

If build succeeds, also verify serve works (for local preview):
```bash
timeout 5 uv run mkdocs serve || true
```

The serve command should start without errors (timeout prevents it from running indefinitely).
  </action>
  <verify>cd /home/luke/workspace/github.com/lukemcguire/vibraphone && uv run mkdocs build 2>&1 | tail -5 && test -d site && test -f site/index.html && echo "BUILD OK"</verify>
  <done>MkDocs build succeeds; site/ directory contains rendered HTML; all tool pages present</done>
</task>

</tasks>

<verification>
- [ ] mkdocs.yml configured with mkdocstrings
- [ ] docs/index.md landing page exists
- [ ] docs/configuration.md has full field reference
- [ ] docs/tools/*.md has API reference for all tools
- [ ] docs/architecture.md explains system design
- [ ] docs/errors.md documents all error types
- [ ] MkDocs build succeeds
- [ ] site/ directory contains rendered documentation
</verification>

<success_criteria>
MkDocs documentation site builds successfully with auto-generated API reference from docstrings. All MCP tools documented with parameters. Architecture and error reference complete.
</success_criteria>

<output>
After completion, create `.planning/phases/09-testing-documentation/09-05-SUMMARY.md`
</output>
