---
phase: 09-testing-documentation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_phase5_success.py
autonomous: true
requirements:
  - TEST-01

must_haves:
  truths:
    - "All Phase 5 success criteria tests pass"
    - "get_execution_context mocked to return tuple (Path, SessionState | None)"
    - "Tests no longer fail with unpacking errors"
  artifacts:
    - path: "tests/test_phase5_success.py"
      provides: "Phase 5 success criteria verification"
      contains: "get_execution_context"
  key_links:
    - from: "tests/test_phase5_success.py"
      to: "src/vibraphone/utils/context.py"
      via: "mock patch"
      pattern: "get_execution_context.*return_value.*tuple"
---

<objective>
Fix the test_phase5_success.py regression introduced by Phase 8's get_execution_context change.

Purpose: Phase 8 changed get_execution_context() to return tuple[Path, SessionState | None] instead of just Path. The test_phase5_success.py tests mock get_project_root directly but the tools now use get_execution_context, causing tests to fail with unpacking errors.

Output: All Phase 5 success criteria tests passing with properly mocked get_execution_context.
</objective>

<execution_context>
@/home/luke/.claude/get-shit-done/workflows/execute-plan.md
@/home/luke/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Phase 8 context (critical for understanding regression)
@.planning/phases/08-quality-gate-worktree-integration/08-01-SUMMARY.md

# Source files showing the signature change
@src/vibraphone/utils/context.py
@tests/test_phase5_success.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Analyze and fix get_execution_context mock patches in test_phase5_success.py</name>
  <files>tests/test_phase5_success.py</files>
  <action>
Update all test methods in test_phase5_success.py that mock `get_project_root` to instead mock `get_execution_context` with the correct tuple return format.

The current tests patch:
```python
mocker.patch("vibraphone.tools.quality_gate_tools.get_project_root", return_value=tmp_path)
```

After Phase 8, the quality gate tools no longer call `get_project_root` directly. They call `get_execution_context()` which returns `tuple[Path, SessionState | None]`. Tests must be updated to:

1. Replace `get_project_root` mocks with `get_execution_context` mocks
2. Return proper tuple: `return_value = (tmp_path, None)` for no-session cases
3. Patch at correct location: `vibraphone.tools.quality_gate_tools.get_execution_context`

Affected tests in test_phase5_success.py:
- test_QUAL_01_run_tests_with_circuit_breaker (line 45)
- test_QUAL_02_run_lint (line 88)
- test_QUAL_03_run_format (line 119)
- test_QUAL_04_request_code_review (line 157)
- test_QUAL_05_attempt_commit_requires_review (line 213)
- test_QUAL_06_circuit_breaker_in_tools (line 317)

DO NOT change the test assertions - only fix the mock patches to work with the new get_execution_context signature.
  </action>
  <verify>cd /home/luke/workspace/github.com/lukemcguire/vibraphone && uv run pytest tests/test_phase5_success.py -v</verify>
  <done>All 8 tests in test_phase5_success.py pass with 0 failures</done>
</task>

<task type="auto">
  <name>Task 2: Add helper fixture for get_execution_context mocking</name>
  <files>tests/test_phase5_success.py</files>
  <action>
Add a reusable fixture or helper method to TestPhase5SuccessCriteria class that standardizes the get_execution_context mock pattern.

Create either:
Option A (preferred - class method):
```python
def _mock_execution_context(self, mocker, tmp_path, session=None):
    """Helper to mock get_execution_context with proper tuple return."""
    mock = mocker.patch("vibraphone.tools.quality_gate_tools.get_execution_context")
    mock.return_value = (tmp_path, session)
    return mock
```

Option B (pytest fixture in conftest.py if more broadly useful):
Add to tests/conftest.py as `mock_execution_context` fixture.

Use this helper in all test methods that need the mock to reduce duplication and ensure consistency.

After adding the helper, refactor all tests to use it.
  </action>
  <verify>cd /home/luke/workspace/github.com/lukemcguire/vibraphone && uv run pytest tests/test_phase5_success.py -v</verify>
  <done>Helper method/fixture exists and is used in all relevant tests; all tests still pass</done>
</task>

<task type="auto">
  <name>Task 3: Run full test suite to verify no regressions</name>
  <files>tests/test_phase5_success.py</files>
  <action>
Run the complete test suite to ensure the mock changes don't break anything else.

Execute:
1. Run all quality gate tests: `uv run pytest tests/test_quality_gate_tools.py tests/test_phase5_success.py -v`
2. Run context tests: `uv run pytest tests/test_context.py -v`
3. Run full test suite: `uv run pytest -v --tb=short`

If any tests fail, investigate and fix. Common issues:
- Mock patch location wrong (patch where used, not where defined)
- Return value format incorrect (must be tuple)
- Session state not properly created when needed
  </action>
  <verify>cd /home/luke/workspace/github.com/lukemcguire/vibraphone && uv run pytest -v --tb=short 2>&1 | tail -20</verify>
  <done>All tests pass; test_phase5_success.py tests included in passing count</done>
</task>

</tasks>

<verification>
- [ ] All tests in test_phase5_success.py pass
- [ ] get_execution_context mocked with tuple return format in all relevant tests
- [ ] Helper method/fixture created for reusable mock pattern
- [ ] Full test suite passes (no regressions)
</verification>

<success_criteria>
All Phase 5 success criteria tests pass with properly mocked get_execution_context that returns tuple[Path, SessionState | None].
</success_criteria>

<output>
After completion, create `.planning/phases/09-testing-documentation/09-01-SUMMARY.md`
</output>
