---
phase: 10-command-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/vibraphone/commands/__init__.py
  - src/vibraphone/commands/v.md
autonomous: true
requirements:
  - SLASH-01
  - SLASH-02
  - SLASH-03

must_haves:
  truths:
    - User can invoke core workflow commands via /v (list, next, start, test, lint, format, review, commit, merge, cleanup, complete, recover, status, health)
    - User can invoke dict-heavy commands via /v with proper JSON parameter examples (init, configure-stack, import-plan)
    - MCP Tool Calling Convention section clearly explains dict/list parameter handling
  artifacts:
    - path: src/vibraphone/commands/__init__.py
      provides: Package marker for bundled commands
    - path: src/vibraphone/commands/v.md
      provides: /v slash command documentation
      contains: "MCP Tool Calling Convention"
      min_lines: 100
  key_links:
    - from: v.md
      to: mcp__vibraphone tools
      via: MCP tool name references
      pattern: "vibraphone_"
---

<objective>
Create the bundled v.md slash command file that documents all vibraphone MCP tools
with proper parameter handling conventions.

Purpose: Provides the /v slash command that Claude Code reads to understand how to
route user commands to vibraphone MCP tools.

Output: src/vibraphone/commands/ package with v.md containing command documentation
and MCP Tool Calling Convention section.
</objective>

<execution_context>
@/home/luke/.claude/get-shit-done/workflows/execute-plan.md
@/home/luke/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Reference: Existing SKILL.md content to migrate
@src/vibraphone/skills/v/SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create commands package with __init__.py</name>
  <files>src/vibraphone/commands/__init__.py</files>
  <action>
Create the commands package directory structure. The __init__.py should be empty
(as a simple package marker) or contain only a docstring.

This follows the pattern from src/vibraphone/templates/__init__.py which is
bundled via pyproject.toml artifacts.
  </action>
  <verify>
    - Directory exists: src/vibraphone/commands/
    - File exists: src/vibraphone/commands/__init__.py
    - Package is importable: python -c "import vibraphone.commands"
  </verify>
  <done>
commands/ package created and importable as vibraphone.commands
  </done>
</task>

<task type="auto">
  <name>Task 2: Create v.md slash command file</name>
  <files>src/vibraphone/commands/v.md</files>
  <action>
Create v.md by migrating content from src/vibraphone/skills/v/SKILL.md with these
changes:

1. **Update frontmatter** - change description to reference /v commands pattern:
   ```yaml
   ---
   name: v
   description:
     Vibraphone slash commands for MCP tool orchestration. Use when the user
     invokes /v commands or asks about vibraphone task management.
   argument-hint: <command> [args...] -- init|list|next|start|test|commit|...
   allowed-tools:
     - mcp__vibraphone
   ---
   ```

2. **Keep MCP Tool Calling Convention section** - this addresses SLASH-03 by
   documenting the stringification issue and solution

3. **Keep all command documentation** - addresses SLASH-01 (core workflow) and
   SLASH-02 (dict-heavy tools with proper parameter examples)

4. **Ensure JSON examples use proper format** - NOT stringified:
   - RIGHT: `values: {"language": "go"}`
   - WRONG: `values: "{\"language\": \"go\"}"`

The file should be a complete, self-contained slash command file that Claude
Code can read to understand /v command routing.
  </action>
  <verify>
    - File exists: src/vibraphone/commands/v.md
    - Contains frontmatter with name: v
    - Contains "MCP Tool Calling Convention" section
    - Contains documentation for init, list, next, start, test, lint, format, review, commit, merge, cleanup, complete, recover, status, health
    - Contains dict parameter examples using JSON objects, NOT strings
    - grep -c "vibraphone_" v.md returns >= 15 (tool references)
  </verify>
  <done>
v.md slash command file created with all command documentation and MCP Tool
Calling Convention section
  </done>
</task>

</tasks>

<verification>
- [ ] commands/ package exists and is importable
- [ ] v.md has valid YAML frontmatter with name: v
- [ ] MCP Tool Calling Convention section present
- [ ] All core workflow commands documented (14+ commands)
- [ ] Dict-heavy commands have proper JSON parameter examples
</verification>

<success_criteria>
v.md file ready to be bundled and installed via setup-commands CLI.

This file addresses:
- SLASH-01: Documents all core workflow commands
- SLASH-02: Includes proper parameter handling for dict-heavy tools
- SLASH-03: MCP Tool Calling Convention explains stringification issue
</success_criteria>

<output>
After completion, create `.planning/phases/10-command-infrastructure/10-01-SUMMARY.md`
</output>
