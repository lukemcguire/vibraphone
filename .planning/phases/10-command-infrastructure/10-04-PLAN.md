---
phase: 10-command-infrastructure
plan: 04
type: execute
wave: 1
depends_on: []
files_modified: [README.md]
autonomous: true
gap_closure: true
requirements: [SLASH-04]

must_haves:
  truths:
    - "User can read README to learn how to install /v commands"
    - "User can read README to learn the available /v commands"
    - "User understands MCP Tool Calling Convention for dict/list parameters"
  artifacts:
    - path: "README.md"
      provides: "User-facing /v command documentation"
      contains: "setup-commands"
  key_links:
    - from: "README.md"
      to: "v.md"
      via: "documentation reference"
      pattern: "/v.*command"
---

<objective>
Add /v command documentation to README.md to satisfy SLASH-04 requirement.

Purpose: Users need to know how to install and use the /v slash commands
  without reading internal v.md files.
Output: Updated README.md with installation and usage documentation.
</objective>

<execution_context>
@/home/luke/.claude/get-shit-done/workflows/execute-plan.md
@/home/luke/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Reference for command documentation
@src/vibraphone/commands/v.md

# Gap source
@.planning/phases/10-command-infrastructure/10-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Add Slash Commands section to README.md</name>
  <files>README.md</files>
  <action>
    Add a new section after "Installation" titled "Slash Commands" that includes:

    1. **Installation instructions** for setup-commands CLI:
       - Command: `vibraphone setup-commands`
       - Installs v.md to `~/.claude/commands/v.md`
       - Note: Restart Claude Code after installation

    2. **Available commands** table listing key /v commands:
       - Core workflow: init, list, next, start, test, lint, format, review, commit, merge, cleanup, complete
       - Utilities: status, health, recover
       - Shortcuts: cycle (test-lint-format-review), finish (commit-merge-cleanup-complete)

    3. **MCP Tool Calling Convention** warning box:
       - Explain that dict/list parameters must NOT be stringified
       - Include WRONG vs RIGHT examples matching v.md lines 20-24

    Keep documentation concise - focus on quick reference, not exhaustive details.
    The v.md file (installed by setup-commands) contains complete command documentation.

    Place the section between "Installation" and "Quickstart" to maintain
    the natural reading order (install -> commands -> usage).
  </action>
  <verify>
    grep -q "vibraphone setup-commands" README.md &amp;&amp;
    grep -q "Slash Commands" README.md &amp;&amp;
    grep -q "MCP Tool Calling Convention" README.md
  </verify>
  <done>
    README.md contains setup-commands installation instructions,
    available /v commands summary, and MCP Tool Calling Convention guidance.
  </done>
</task>

</tasks>

<verification>
- grep finds "setup-commands" in README.md
- grep finds "Slash Commands" section header in README.md
- grep finds "MCP Tool Calling Convention" warning in README.md
- README still renders correctly (no broken markdown)
</verification>

<success_criteria>
- README.md has Slash Commands section with installation instructions
- README.md lists available /v commands
- README.md includes MCP Tool Calling Convention guidance
- Documentation is concise and actionable
</success_criteria>

<output>
After completion, create `.planning/phases/10-command-infrastructure/10-04-SUMMARY.md`
</output>
