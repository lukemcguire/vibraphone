---
phase: 10-command-infrastructure
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
autonomous: true
gap_closure: true
requirements: []
user_setup: []

must_haves:
  truths:
    - "README.md references correct CLI command (vibraphone-cli not vibraphone)"
    - "README.md scope remains user-level without protocol implementation details"
    - "Technical MCP content remains in v.md where agents need it"
  artifacts:
    - path: "README.md"
      provides: "User-facing project documentation"
      contains: "vibraphone-cli setup-commands"
      excludes: "MCP Tool Calling Convention section"
  key_links:
    - from: "README.md"
      to: "v.md"
      via: "reference to installed file for complete documentation"
      pattern: "For complete command documentation, see the installed"
---

<objective>
Fix two documentation issues from UAT: correct CLI command name and remove
out-of-scope technical content from README.md.

Purpose: Keep README.md at user-level scope while ensuring accurate command
references. Technical MCP protocol content belongs in v.md (for agents) not
README.md (for users).

Output: Updated README.md with correct CLI command and appropriate scope.
</objective>

<execution_context>
@/home/luke/.claude/get-shit-done/workflows/execute-plan.md
@/home/luke/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix CLI command name in README.md</name>
  <files>README.md</files>
  <action>
    On line 57, change:

    ```bash
    vibraphone setup-commands
    ```

    to:

    ```bash
    vibraphone-cli setup-commands
    ```

    Context: The `vibraphone` command runs the MCP server. The `vibraphone-cli`
    command provides CLI utilities including setup-commands. The README
    incorrectly referenced the MCP server command.
  </action>
  <verify>
    ```bash
    grep -n "vibraphone-cli setup-commands" README.md
    ```
  </verify>
  <done>README.md line 57 shows `vibraphone-cli setup-commands` (not `vibraphone setup-commands`)</done>
</task>

<task type="auto">
  <name>Task 2: Remove MCP Tool Calling Convention section from README.md</name>
  <files>README.md</files>
  <action>
    Remove lines 85-97 (the entire "### MCP Tool Calling Convention" section).

    The removed content includes:
    - Section header: "### MCP Tool Calling Convention"
    - Introductory text about dict/list parameters
    - WRONG/RIGHT table showing JSON serialization examples
    - Note about MCP protocol handling serialization
    - Guidance against manual serialization

    This content is too technical for README scope (user-facing) and is
    already documented in v.md where agents need it. The README should remain
    at a user level, and line 98 ("For complete command documentation, see the
    installed v.md file.") already directs users/agents to v.md for details.

    After removal, the "Available Commands" table (lines 65-83) should be
    directly followed by line 98 ("For complete command documentation...").
  </action>
  <verify>
    ```bash
    # Verify MCP Tool Calling Convention section is gone
    ! grep -q "MCP Tool Calling Convention" README.md

    # Verify Available Commands table is followed by documentation reference
    grep -A1 "^| /v recover" README.md | grep -q "For complete command documentation"
    ```
  </verify>
  <done>README.md no longer contains MCP Tool Calling Convention section. Available Commands table is immediately followed by reference to v.md for complete documentation.</done>
</task>

</tasks>

<verification>
After completing tasks:

1. Verify CLI command is correct:
   ```bash
   grep "vibraphone-cli setup-commands" README.md
   ```

2. Verify MCP Tool Calling Convention section is removed:
   ```bash
   ! grep -q "MCP Tool Calling Convention" README.md
   ```

3. Verify README structure is intact (Available Commands -> v.md reference):
   ```bash
   grep -A1 "^| /v recover" README.md
   ```

4. Run quality checks:
   ```bash
   ruff check . && pytest
   ```
</verification>

<success_criteria>
- README.md references `vibraphone-cli setup-commands` (not `vibraphone setup-commands`)
- README.md no longer contains "MCP Tool Calling Convention" section
- README.md structure intact: Available Commands table followed by v.md reference
- Quality checks pass (ruff, pytest)
</success_criteria>

<output>
After completion, create `.planning/phases/10-command-infrastructure/10-05-SUMMARY.md`
</output>
