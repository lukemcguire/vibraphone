coverage:
  status:
    project:
      default:
        target: auto
        threshold: 5%
    patch:
      default:
        target: auto
        threshold: 5%

ignore:
  - "tests/**"
  - "**/__pycache__/**"
  - "**/site-packages/**"

comment:
  layout: "header, diff, files"
  behavior: default
